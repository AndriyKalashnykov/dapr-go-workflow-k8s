name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_call:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read   # dorny/paths-filter listFiles() on pull_request events

jobs:
  changes:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      # Tag pushes bypass the path filter so the docker publish job always runs.
      # Under act (local `make ci-run`) ACT=true: the filter step is skipped
      # (act's local checkout has no git history for change detection), so default
      # code=true and let the downstream jobs run. Real CI (ACT unset) is unaffected.
      code: ${{ (startsWith(github.ref, 'refs/tags/') || env.ACT == 'true') && 'true' || steps.filter.outputs.code }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4
        if: ${{ !env.ACT }}
        id: filter
        with:
          base: ${{ github.event_name == 'push' && 'main' || '' }}
          filters: |
            code:
              - '!(**.md|docs/**|specs/**|benchmarks/**|LICENSE|.gitignore|.claudeignore|.claude/**|**.png|**.jpg|**.gif|**.svg)'
              - 'CLAUDE.md'

  static-check:
    needs: [changes]
    if: |
      !failure() && !cancelled() &&
      (needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/'))
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          fetch-depth: 0   # gitleaks scans git history
      - uses: jdx/mise-action@dba19683ed58901619b14f395a24841710cb4925 # v4
        with:
          # Pinned: mise resolves "latest" to a tag whose release assets may not
          # be published yet (v2026.6.7 was tagged with no linux-x64 asset → 404).
          version: "2026.6.5"
          install: true
          cache: true
      - name: Cache Go modules
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: ~/go/pkg/mod
          key: go-mod-${{ runner.os }}-${{ hashFiles('go.sum') }}
          restore-keys: go-mod-${{ runner.os }}-
      - name: Static check
        run: make static-check

  build:
    needs: [changes, static-check]
    if: |
      !failure() && !cancelled() &&
      (needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/'))
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: jdx/mise-action@dba19683ed58901619b14f395a24841710cb4925 # v4
        with:
          # Pinned: mise resolves "latest" to a tag whose release assets may not
          # be published yet (v2026.6.7 was tagged with no linux-x64 asset → 404).
          version: "2026.6.5"
          install: true
          cache: true
      - name: Cache Go modules
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: ~/go/pkg/mod
          key: go-mod-${{ runner.os }}-${{ hashFiles('go.sum') }}
          restore-keys: go-mod-${{ runner.os }}-
      - name: Build
        run: make build

  test:
    needs: [changes, static-check]
    if: |
      !failure() && !cancelled() &&
      (needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/'))
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: jdx/mise-action@dba19683ed58901619b14f395a24841710cb4925 # v4
        with:
          # Pinned: mise resolves "latest" to a tag whose release assets may not
          # be published yet (v2026.6.7 was tagged with no linux-x64 asset → 404).
          version: "2026.6.5"
          install: true
          cache: true
      - name: Cache Go modules
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: ~/go/pkg/mod
          key: go-mod-${{ runner.os }}-${{ hashFiles('go.sum') }}
          restore-keys: go-mod-${{ runner.os }}-
      - name: Test
        run: make test
      - name: Verify coverage threshold
        run: make coverage-check

  e2e:
    # Runs the workflow through a real Dapr sidecar against REAL backends:
    # `make e2e` does `dapr init` (Docker control plane), starts PostgreSQL, and
    # e2e/e2e-test.sh creates a kind cluster, runs the app, schedules
    # PostgresSQLDatabasesPut and asserts the recipe actually provisioned a real
    # Postgres role+database (the returned URI authenticates) and a real
    # Kubernetes Service+Secret, then PostgresSQLDatabasesDelete tears both down.
    # kind + kubectl come from mise. Not run under act (Docker-in-Docker
    # `dapr init`/kind is unreliable there); `make ci-run` exercises only
    # static-check/build/test.
    needs: [changes, build, test]
    if: |
      !failure() && !cancelled() &&
      (needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/'))
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: jdx/mise-action@dba19683ed58901619b14f395a24841710cb4925 # v4
        with:
          # Pinned: mise resolves "latest" to a tag whose release assets may not
          # be published yet (v2026.6.7 was tagged with no linux-x64 asset → 404).
          version: "2026.6.5"
          install: true
          cache: true
      - name: Cache Go modules
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5
        with:
          path: ~/go/pkg/mod
          key: go-mod-${{ runner.os }}-${{ hashFiles('go.sum') }}
          restore-keys: go-mod-${{ runner.os }}-
      - name: Run end-to-end tests
        run: make e2e

  docker:
    # Runs on every code-changing push (build + Trivy scan). Builds linux/amd64
    # only (arm64 dropped to keep CI fast — no QEMU emulation). Login / push /
    # cosign are tag-gated at step level so the
    # image is only published on a vN.N.N tag. No standalone smoke test: this
    # service requires a Dapr sidecar to reach a healthy state, so a bare
    # `docker run` cannot pass — the runtime workflow path is covered by the
    # `e2e` job above.
    needs: [changes, static-check, build, test]
    if: |
      !failure() && !cancelled() &&
      (needs.changes.outputs.code == 'true' || startsWith(github.ref, 'refs/tags/'))
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      packages: write   # exercised only by the tag-gated push step
      id-token: write   # exercised only by the tag-gated cosign step
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@80c7e94dd9b9319bd5eb7a0e0fe9291e23a2a2e9 # v6
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=ref,event=branch
            type=ref,event=pr
          flavor: |
            latest=${{ startsWith(github.ref, 'refs/tags/') }}

      - name: Build image for scan
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7
        with:
          context: .
          platforms: linux/amd64
          load: true
          tags: dapr-go-workflow-k8s:scan
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Trivy image scan
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: 'dapr-go-workflow-k8s:scan'
          scanners: 'vuln,secret,misconfig'
          severity: 'CRITICAL,HIGH'
          exit-code: '1'
          ignore-unfixed: true

      - name: Log in to GHCR
        if: startsWith(github.ref, 'refs/tags/')
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        id: push
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7
        with:
          context: .
          platforms: linux/amd64
          push: ${{ startsWith(github.ref, 'refs/tags/') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: false
          sbom: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install cosign
        if: startsWith(github.ref, 'refs/tags/')
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Sign image with cosign
        if: startsWith(github.ref, 'refs/tags/')
        env:
          TAGS: ${{ steps.meta.outputs.tags }}
          DIGEST: ${{ steps.push.outputs.digest }}
        run: |
          set -euo pipefail
          while IFS= read -r tag; do
            [ -z "$tag" ] && continue
            cosign sign --yes "${tag}@${DIGEST}"
          done <<< "${TAGS}"

  ci-pass:
    if: always()
    needs: [changes, static-check, build, test, e2e, docker]
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Verify all jobs passed
        run: |
          if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" ]]; then
            echo "One or more jobs failed"; exit 1
          fi
          if [[ "${{ contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "One or more jobs were cancelled"; exit 1
          fi
          echo "All required jobs passed or were skipped."
